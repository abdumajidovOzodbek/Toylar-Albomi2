{
  "buildCommand": "pnpm --filter @workspace/wedding-memory run build && cp -r artifacts/wedding-memory/dist/public ./public",
  "outputDirectory": "public",
  "installCommand": "pnpm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
