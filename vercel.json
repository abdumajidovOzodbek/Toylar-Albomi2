{
  "buildCommand": "pnpm --filter @workspace/wedding-memory run build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
